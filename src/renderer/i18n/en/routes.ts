import { RoutesMessages } from '../types'

const messages: RoutesMessages = {
  'routes.invalid.asset': 'Invalid asset in route: {asset}',
  'routes.invalid.params': 'Invalid route parameter(s) {params}'
}

export default messages
