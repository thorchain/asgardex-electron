import { PoolSharesMessage } from '../types'

const poolShares: PoolSharesMessage = {
  'poolshares.ownership': 'Ownership',
  'poolshares.both.info': 'Both side shares ({asset} and {rune})',
  'poolshares.single.info': 'Single side share ({asset} or {rune})',
  'poolshares.single.notsupported':
    'Single side pool shares ({asset} or {rune}) are not supported in ASGARDEX currently'
}

export default poolShares
