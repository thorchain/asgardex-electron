import { UpdateMessages } from '../types'

const update: UpdateMessages = {
  'update.available': 'New version is available',
  'update.description': 'There is a new version {version} available. You can go to the releases page and download it'
}

export default update
