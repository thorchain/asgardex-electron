import { UpdateMessages } from '../types'

const update: UpdateMessages = {
  'update.available': 'New update available',
  'update.description':
    'There is a new version {version} available. Click "Ok" to go to the release page, where you can download it.'
}

export default update
