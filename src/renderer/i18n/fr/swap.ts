import { SwapMessages } from '../types'

const swap: SwapMessages = {
  'swap.swapping': 'You are swapping - FR',
  'swap.input': 'Input - FR',
  'swap.balance': 'Balance - FR',
  'swap.output': 'Output - FR',
  'swap.drag': 'Drag to swap - FR'
}

export default swap
