import { RoutesMessages } from '../types'

const messages: RoutesMessages = {
  'routes.invalid.asset': 'Недопустимый актив в маршруте: {asset}',
  'routes.invalid.params': 'Недопустимый(е) параметр(ы) маршрута {params}'
}

export default messages
