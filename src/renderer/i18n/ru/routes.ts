import { RoutesMessages } from '../types'

const messages: RoutesMessages = {
  'routes.invalid.asset': 'Недопустимый актив в пути: {asset}',
  'routes.invalid.params': 'Недопустимый параметр(ы) маршрута {params}'
}

export default messages
