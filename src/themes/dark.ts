import { Theme } from './types'
import { theme as defaultTheme } from './default'

/**
 * Dark theme:
 * It overrides of default theme definitions
 */
export const theme: Theme = {
  ...defaultTheme
}
