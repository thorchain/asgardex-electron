import React from 'react'

import { storiesOf } from '@storybook/react'

import HeaderTheme from './HeaderTheme'

storiesOf('Components/HeaderTheme', module).add('default', () => {
  return <HeaderTheme />
})
