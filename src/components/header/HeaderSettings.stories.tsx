import React from 'react'

import { storiesOf } from '@storybook/react'

import HeaderSettings from './HeaderSettings'

storiesOf('Components/HeaderSettings', module).add('default', () => {
  return <HeaderSettings />
})
