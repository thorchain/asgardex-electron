import React from 'react'

import { storiesOf } from '@storybook/react'

import HeaderLock from './HeaderLock'

storiesOf('Components/HeaderLock', module).add('default', () => {
  return <HeaderLock />
})
