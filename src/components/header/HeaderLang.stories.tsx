import React from 'react'

import { storiesOf } from '@storybook/react'

import HeaderLang from './HeaderLang'

storiesOf('Components/HeaderLang', module).add('default', () => {
  return <HeaderLang />
})
